<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Welcome!</title>

    <!-- Balises pour le responsive -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">

    <!-- Notre CSS custom -->
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
</head>

<body class="mb-5">
    <nav class="container py-3 mb-3">
        <ul class="nav nav-pills">
            <li class="nav-item">
                <a class="nav-link {% if title == 'Bienvenue sur la météo des villes !' %} active {% endif %} " href="{{path ('homepage')}}" >Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if title =='A la montagne' %} active {% endif %} " href="{{path ('mountainpage')}}">Montagne</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if title =='La météo des plages' %} active {% endif %} " href="{{path ('beachpage')}}">Plages</a>
            </li>
        </ul>
    </nav>

    <!-- Minimal responsive + columns -->
    <main class="container">
        <div class="row">
              {% block body %}{% endblock %}
 
{% include('main/_sidebar.html.twig') %}


{# {{ session = app.session.get("choosenCity",[])}} #}
       {# {{ dump(app.session)}} #}
            
        </div>
    </main>

</body>

</html>